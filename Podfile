# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

source 'https://github.com/egouletlang/BaseUtilsSpecs.git'
source 'https://github.com/egouletlang/PodUISpecs.git'
source 'https://github.com/egouletlang/PodImageSpecs.git'

target 'PodLocation' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PodLocation
  pod 'BaseUtils'
  pod 'PodUI'

  target 'PodLocationTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
